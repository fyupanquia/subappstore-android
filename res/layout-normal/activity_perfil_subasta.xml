<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:background="@drawable/background2"
    android:paddingBottom="@dimen/activity_vertical_margin"
    android:paddingLeft="@dimen/activity_horizontal_margin"
    android:paddingRight="@dimen/activity_horizontal_margin"
    android:paddingTop="@dimen/activity_vertical_margin"
    tools:context="com.sise.subappstore.PerfilSubasta" >

    <ListView
        android:id="@+id/lvImagenesPerfilSubasta"
        android:layout_width="400dp"
        android:layout_height="280dp"
        android:layout_centerHorizontal="true"
        android:layout_centerVertical="true" >
    </ListView>

    <ListView
        android:id="@+id/lvDescripcionPerfiLSubasta"
        android:layout_width="match_parent"
        android:layout_height="150dp"
        android:layout_alignLeft="@+id/lvImagenesPerfilSubasta"
        android:layout_below="@+id/lvImagenesPerfilSubasta"
        android:layout_marginTop="34dp" >
    </ListView>

    <TextView
        android:id="@+id/tvNombrePerfilSubasta"
        android:layout_width="180dp"
        android:layout_height="wrap_content"
        android:layout_alignLeft="@+id/tvCategoriaPerfilSubasta"
        android:layout_alignParentTop="true"
        android:layout_marginTop="14dp"
        android:textSize="24sp" />

    <TextView
        android:id="@+id/tvCategoriaPerfilSubasta"
        android:layout_width="180dp"
        android:layout_height="wrap_content"
        android:layout_above="@+id/lvImagenesPerfilSubasta"
        android:layout_alignLeft="@+id/lvImagenesPerfilSubasta"
        android:textSize="18sp" />

    <TextView
        android:id="@+id/tvPrecioPerfilSubasta"
        android:layout_width="100dp"
        android:layout_height="wrap_content"
        android:layout_alignBaseline="@+id/tvCategoriaPerfilSubasta"
        android:layout_alignBottom="@+id/tvCategoriaPerfilSubasta"
        android:layout_alignRight="@+id/lvImagenesPerfilSubasta"
        android:textSize="24sp" />

</RelativeLayout>